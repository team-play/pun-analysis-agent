# Claude Code Instructions

This project's agent instructions live in [`AGENTS.md`](AGENTS.md) — read that file first. It covers discovery (where the spec/contracts/setup docs are), the coding guide, the code review requirement, and performance constraints. Nothing is duplicated here so the two files can't drift out of sync.
