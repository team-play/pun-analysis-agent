# Placeholder workflow for the Inference Cloud Run service.
#
# This will build the Docker image in `inference/` and deploy it to Cloud Run
# once the service has a real, working Dockerfile and build step to deploy.
# No deploy logic yet — filled in in a follow-up pass.

on:
  push:
    branches: [main]
    paths:
      - "inference/**"
